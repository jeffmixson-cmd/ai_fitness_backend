# AI Fitness Backend

This is the FastAPI + Supabase backend for the AI Fitness Coach project.

## Run locally

1. Create a `.env` file and fill in:
   - SUPABASE_URL
   - SUPABASE_SERVICE_ROLE_KEY

2. Install dependencies:
   ```bash
   pip install -r requirements.txt
